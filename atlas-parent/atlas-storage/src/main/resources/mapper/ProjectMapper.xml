<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atlas.storage.repository.ProjectMapper">

    <resultMap id="BaseResultMap" type="com.atlas.core.project.Project">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="workspace_id" property="workspaceId"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, description, status, created_at, updated_at, workspace_id
    </sql>

    <select id="selectByWorkspaceId" parameterType="string" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM project
        WHERE workspace_id = #{workspaceId}
    </select>

    <select id="selectByStatus" parameterType="com.atlas.core.project.Project$ProjectStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM project
        WHERE status = #{status}
    </select>

</mapper>
